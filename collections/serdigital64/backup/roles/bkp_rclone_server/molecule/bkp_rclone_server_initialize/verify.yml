---
- name: "Verify"
  hosts: "{{ lookup('env', 'DEVAP_TEST_PLATFORM') | default('all') }}"
  gather_facts: false
  tasks:
    - name: "Discover platform"
      ansible.builtin.setup:
        gather_subset: "min"
        gather_timeout: "10"

    - name: "Load role variables"
      ansible.builtin.include_role:
        public: true
        name: "bkp_rclone_server"

    - name: "Verify that core variables are set"
      ansible.builtin.assert:
        that:
          - bkp_rclone_server_platforms is defined
          - bkp_rclone_server_profiles is defined
          - bkp_rclone_server_application is defined
...
