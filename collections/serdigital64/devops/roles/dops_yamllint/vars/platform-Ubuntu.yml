---
# Define platform specific paths
dops_yamllint_base:
  paths: "{{ dops_yamllint_base_common['paths'] }}"
  etc: "{{ dops_yamllint_base_common['etc'] }}"
  files: "{{ dops_yamllint_base_common['files'] }}"
  run: "{{ dops_yamllint_base_common['run'] }}"
  templates: "{{ dops_yamllint_base_common['templates'] }}"
  access: "{{ dops_yamllint_base_common['access'] }}"

# Define application package details
dops_yamllint_profiles:
  snap: "{{ dops_yamllint_profiles_common['snap'] }}"
  flatpak: "{{ dops_yamllint_profiles_common['flatpak'] }}"
  pip: "{{ dops_yamllint_profiles_common['pip'] }}"
  distro: {}

# Define custom package repositories
dops_yamllint_repository_custom_yum: []
dops_yamllint_repository_custom_apt: []

# Set package repositories states
dops_yamllint_repository_catalog_distro: {}
dops_yamllint_repository_catalog_yum: {}
dops_yamllint_repository_catalog_apt: {}
...
