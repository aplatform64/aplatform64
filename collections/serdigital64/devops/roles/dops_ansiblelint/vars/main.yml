---
# Define supported platforms
dops_ansiblelint_platforms:
  - "CentOS8"
  - "OracleLinux8"
  - "Ubuntu20"
  - "Ubuntu21"
  - "Fedora33"
  - "Fedora35"
  - "Debian10"
  - "Debian11"

# Group Linux Distributions by family
dops_ansiblelint_os_family: "
  {%- if
      ( ansible_distribution == 'CentOS' ) or
      ( ansible_distribution == 'OracleLinux' ) or
      ( ansible_distribution == 'Fedora' )
  -%}
    {{ 'RedHat' }}
  {%- elif
      ( ansible_distribution == 'Debian' ) or
      ( ansible_distribution == 'Ubuntu' )
  -%}
    {{ 'Ubuntu' }}
  {%- else -%}
    {{ 'Linux' }}
  {%- endif -%}
  "

# Multiplatform packages
dops_ansiblelint_profiles_common:
  snap: {}
  flatpak: {}
  pip:
    latest:
      supported: true
      packages:
        - name: "ansible-lint"
      options:
        pip:
          owner: "{{ dops_ansiblelint_owners['app']['name'] }}"
          group: "{{ dops_ansiblelint_owners['app']['group'] }}"
          path: "{{ dops_ansiblelint_paths['root'] }}"
          cli: "ansible-lint"

# Define common paths
dops_ansiblelint_base_common:
  access:
    user:
      owner: "root"
      app: "{{ dops_ansiblelint_owners['app']['name'] }}"
    group:
      owner: "root"
      app: "{{ dops_ansiblelint_owners['app']['group'] }}"
    mode:
      dir: "0755"
      file: "0644"
      exe: "0755"
  templates: {}
  files: {}
  etc: {}
  paths:
    root: "{{ dops_ansiblelint_paths['root'] }}"
  run:
    cli: "{{ dops_ansiblelint_paths['root'] + '/ansible-lint' }}"
...
