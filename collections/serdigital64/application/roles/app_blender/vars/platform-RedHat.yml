---
# Define platform specific paths
app_blender_base:
  paths: "{{ app_blender_base_common['paths'] }}"
  etc: "{{ app_blender_base_common['etc'] }}"
  files: "{{ app_blender_base_common['files'] }}"
  run: "{{ app_blender_base_common['run'] }}"
  templates: "{{ app_blender_base_common['templates'] }}"
  access: "{{ app_blender_base_common['access'] }}"

# Define application package details
app_blender_profiles:
  snap: "{{ app_blender_profiles_common['snap'] }}"
  flatpak: "{{ app_blender_profiles_common['flatpak'] }}"
  distro: {}

# Define custom package repositories
app_blender_repository_custom_yum: []
app_blender_repository_custom_apt: []

# Set package repositories states
app_blender_repository_catalog_distro: {}
app_blender_repository_catalog_yum: {}
app_blender_repository_catalog_apt: {}
...
