---
- name: "System / GUI / Cinnamon / Provision / Configuration / Create user configuration"
  become: true
  ansible.builtin.template:
    backup: false
    follow: false
    force: true
    unsafe_writes: false
    owner: "{{ sys_gui_cinnamon_base['access']['user']['owner'] }}"
    group: "{{ sys_gui_cinnamon_base['access']['group']['owner'] }}"
    mode: "{{ sys_gui_cinnamon_base['access']['mode']['file'] }}"
    src: "{{ sys_gui_cinnamon_base['templates'][sys_gui_cinnamon_users['default']] }}"
    dest: "{{ sys_gui_cinnamon_base['paths']['var'] + '/' + sys_gui_cinnamon_users['default'] }}"

- name: "System / GUI / Cinnamon / Provision / Configuration / Deploy user configuration to DConf"
  vars:
    sys_dconf:
      provision: true
    sys_dconf_db_local:
      - source: "{{ sys_gui_cinnamon_base['paths']['var'] + '/' + sys_gui_cinnamon_users['default'] }}"
        name: "{{ 'sys_gui_cinnamon-' + sys_gui_cinnamon_users['default'] }}"
  ansible.builtin.include_role:
    name: "serdigital64.system.sys_dconf"
...
