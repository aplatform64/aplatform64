# {{ infra_terraform_alert_ansible_managed_file }}

plugin_cache_dir = "{{ infra_terraform_base['paths']['cache'] }}"

{# provider_installation {
  filesystem_mirror {
    path    = "{{ infra_terraform_base['paths']['providers'] }}"
    include = ["{{ infra_terraform_providers['libvirt']['registry'] }}"]
  }
  direct {
    exclude = ["{{ infra_terraform_providers['libvirt']['registry'] }}"]
  }
} #}